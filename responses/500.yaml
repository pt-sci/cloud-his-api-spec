description: INTERNAL SERVER ERROR
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: number
        message:
          type: string
